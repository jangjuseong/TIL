def solution(elements):
    results = []
    answer = 0
    circle_elements = elements + elements

    length = len(elements)

    for i in range(1, length+1):
        for j in circle_elements:
            pass
        # 여기서 더 생각 못함

    return answer
